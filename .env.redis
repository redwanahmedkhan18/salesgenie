# Redis Configuration for SalesGenie
# Since Redis is not available in this environment, the application will use
# an in-memory mock for development

# For production/local development with Redis:
# Redis Server: localhost:6380 (or 6379)
# Redis Password: none (unless configured)

REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=0