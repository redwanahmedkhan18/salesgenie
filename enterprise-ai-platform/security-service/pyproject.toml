[project]
name = "salesgenie-security-service"
version = "1.0.0"
description = "Security & Governance service for SalesGenie — AI Security, MCP Security, threat detection"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
