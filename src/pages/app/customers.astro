---
import Layout from '../layouts/Layout.astro';
---

<Layout title="SalesGenie Customers — Customer Management">
  <div id="customers-root"></div>
</Layout>

<script type="module">
  import { AuthProvider } from '../../auth/AuthProvider';
  import { ProtectedRoute } from '../../auth/ProtectedRoute';
  import CustomerManagement from '../../components/islands/CustomerManagement';
  import React from 'react';
  import { createRoot } from 'react-dom/client';

  const root = createRoot(document.getElementById('customers-root')!);
  root.render(
    React.createElement(AuthProvider, null,
      React.createElement(ProtectedRoute, { requiredRoles: ['sales_manager', 'sales_agent', 'support_manager', 'support_agent', 'org_admin', 'super_admin'] },
        React.createElement(CustomerManagement, null)
      )
    )
  );
</script>